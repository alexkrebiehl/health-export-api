# Copy to .env and replace with a newly generated secret. Never commit .env.
HEALTH_EXPORT_API_TOKEN=replace-with-a-random-token
# Seconds to cache route-coverage results. 0 disables caching.
HEALTH_EXPORT_CACHE_TTL=300
PUID=1000
PGID=1000
